<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:shootarama"
             x:Class="shootarama.MainPage"
             BackgroundImage="Images/MenuBG.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
 
        <Image Grid.Row="0" HorizontalOptions="Center" Source="Images/logo.png" />
        
        <Grid Grid.Row="1" Margin="0,20,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <Button Grid.Row="0" Text="New Game" Style="{StaticResource MainButton}" />
            <Button Grid.Row="1" Text="Load Game" Style="{StaticResource MainButton}" />
            <Button Grid.Row="2" Text="About" Style="{StaticResource MainButton}" Clicked="btnAbout_Click" />
        </Grid>
        
        <Label Grid.Row="2" VerticalOptions="End" HorizontalOptions="Center" FontSize="20" TextColor="White"  Text="(C)1995-2017 Jarred Capellman." />
    </Grid>
</ContentPage>